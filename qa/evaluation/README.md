# QA Evaluation

This directory stores offline regression cases for the QA agent. These cases are
not part of the runtime path; they document expected routing, follow-up, and
tool-selection behavior so prompt or routing changes can be checked later.
